# Minimum-version floors rather than exact pins: this deploys to whatever
# modern Python the host provides (Render defaults to a current 3.x), and
# floors avoid build failures from a pinned patch release being unavailable.
# Pin exact versions here once the deployment is stable.
fastapi>=0.110
uvicorn[standard]>=0.29
SQLAlchemy>=2.0
pydantic>=2.6
requests>=2.31
python-dotenv>=1.0
youtube-transcript-api>=0.6.2

# Postgres driver (unused when DATABASE_URL is unset and SQLite is the fallback)
psycopg2-binary>=2.9.9

# Timezone data for the ET-anchored publishing slots (app/slots.py). zoneinfo
# needs tzdata where the OS does not ship it; pytz is the fallback for older Pythons.
tzdata>=2024.1
pytz>=2023.3
